syntax = "proto3";

package test.v1;

// Used as both RPC request and a field type.
message SharedType {
  string val = 1;
}

message Wrapper {
  SharedType inner = 1;
}

message Response {
  string val = 1;
}

service Svc {
  rpc DoThing(SharedType) returns (Response);
}
